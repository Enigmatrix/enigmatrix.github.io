@tailwind preflight;
@tailwind components;
@tailwind utilities;

@css{
	section {
		@apply p-6;
	}
}

.whoami
	background: linear-gradient(to left, #00b4db, #0083b0);
	color: white;

html
	line-height: 1.2;
	
